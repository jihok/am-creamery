<div class="row">
	<div class="span5">
		<h2>Incomplete Shifts</h2>
		<%= render :partial => "incomplete_shifts" unless @shifts.empty? %>
	</div>
	<div class="span7">
		<h2>Completed Shifts</h2>
		<%= render :partial => "completed_shifts" unless @completed_shifts.empty? %>
		
	</div>
</div>

<%= link_to raw("<i class='icon-plus icon-white'></i> Add a New Shift"), new_shift_path, :class => 'btn btn-primary', :id => 'Create Shift' %>
